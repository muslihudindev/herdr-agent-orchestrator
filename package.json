{
  "name": "agent-orchestration",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first provider-agnostic multi-agent AI development platform orchestrated through HerdR and the real Pi TUI.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "rm -rf dist && npm run build && node --test dist/tests/*.test.js",
    "install-extension": "scripts/install-pi-extension.sh"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {}
}
